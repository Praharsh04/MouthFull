import ctypes
from ctypes import wintypes
import winreg
from PIL import Image

# ... (I'll implement the full ctypes version if I have to, but let's test if we can just use `pywin32` by installing it first)
