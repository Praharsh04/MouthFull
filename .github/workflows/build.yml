name: CI / CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  release:
    types: [created]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
    - name: Run Tests
      run: |
        # pytest tests/ (Tests not fully implemented yet)
        echo "Skipping tests for now"
    - name: Build Executable
      run: |
        python scripts/build.py --config Release
    - name: Build Installer
      run: |
        & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss
    - name: Create Portable ZIP
      run: |
        New-Item -ItemType Directory -Force -Path release
        Compress-Archive -Path dist/Release/MouthFull/* -DestinationPath release/MouthFull_Local_Portable.zip
    - name: Upload Artifacts
      if: github.event_name != 'release'
      uses: actions/upload-artifact@v4
      with:
        name: MouthFull-Release
        path: |
          release/MouthFull_Local_Setup.exe
          release/MouthFull_Local_Portable.zip
    - name: Publish Release Assets
      if: github.event_name == 'release'
      uses: softprops/action-gh-release@v1
      with:
        files: |
          release/MouthFull_Local_Setup.exe
          release/MouthFull_Local_Portable.zip
